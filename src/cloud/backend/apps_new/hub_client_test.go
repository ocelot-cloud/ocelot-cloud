package apps_new

import (
	"testing"
)

// TODO All the hub client data are made for tests. I think I need a createHubClient function in which I set all params for production use.
// TODO Implement and test via client: search apps, download app
// TODO Create ci-runner test: 1) run hub, then 2) run cloud client tests
// TODO Create a mock for this client: 1) hubClientReal and 2) hubClientMock, also add ENV in order to switch between them
func TestRegister(t *testing.T) {
	/*client := hub.GetHub()
	client.RegisterUser()*/
}
